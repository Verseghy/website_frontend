name: Verseghy CI
on: [push]

jobs:
  testall:
    name: Lint, Build, Test, E2E
    strategy:
      matrix:
        node: [12]
        os: [macOS-latest, windows-latest, ubuntu-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v1

      - name: Setup node
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node }}

      - name: Setup dependencies
        run: yarn

      - name: Lint
        run: yarn nx affected:lint --all

      - name: Format
        run: yarn nx format:check --all

      - name: Build
        run: yarn affected:build --prod --no-progress --all

      - name: Test
        run: yarn affected:test --no-progress --all

      - name: E2E
        run: yarn nx affected:e2e --prod --all
